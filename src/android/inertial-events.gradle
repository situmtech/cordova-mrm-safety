def inertialVersion = "1.4.0"

repositories {
    maven { url "https://repo.situm.es/artifactory/libs-release-local" }
    google()
    mavenCentral()
}

dependencies {
    implementation "es.situm:inertial-events-detection:${inertialVersion}@aar"
}
